def inverte_lista (lista: list) -> list:
    resposta = []
    for i in range (len(lista)-1,-1,-1):
        resposta.append(lista[i])
    return resposta
lista = for z in range (0,1000)
print(inverte_lista(lista))